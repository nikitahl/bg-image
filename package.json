{
  "name": "bg-image",
  "version": "1.0.0",
  "description": "Prototype, modify and edit a background image position, size, etc. and get a ready to use CSS code for your project",
  "repository": "https://github.com/nikitahl/bg-image.git",
  "author": "nikitahl",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel": "^2.0.0",
    "parcel-plugin-meta-images": "github:plumdumpling/parcel-plugin-meta-images"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
